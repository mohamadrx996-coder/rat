{
  "name": "ops-terminal",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd api && node server.js",
    "build": "cd client && npm run build"
  }
}
